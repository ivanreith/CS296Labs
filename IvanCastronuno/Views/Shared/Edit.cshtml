@model StoriesModelForm
@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

@{
    string title = ViewBag.Action + " Story";
    ViewBag.Title = title;
}

<h2>@ViewBag.Title</h2>

<form asp-action="Edit" method="post">
    <div asp-validation-summary="All" class="text-danger"></div>

    <div class="form-group">
        <label asp-for="StoryTitle">Title</label>
        <input asp-for="StoryTitle" class="form-control">
        <span asp-validation-for="StoryTitle" class="text-danger"></span>
    </div>

    <div class="form-group">
        <label asp-for="StoryTopic">Topic</label>
        <input asp-for="StoryTopic" class="form-control">
        <span asp-validation-for="StoryTopic" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="StoryText">Text</label>
        <input asp-for="StoryText" class="form-control">
        <span asp-validation-for="StoryText" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Poster.UserName">Poster</label>
        <select asp-for="Poster.UserName" class="form-control">
            <option value="">select a User</option>
            @foreach (AppUser g in ViewBag.Users)
            {
                <option value="@g.Id">@g.Name</option>
            }
        </select>
    </div>



    <input type="hidden" asp-for="StoryID" value="0" />

    <button type="submit" class="btn btn-primary">@ViewBag.Action</button>
    <a asp-controller="Home" asp-action="Stories"
       class="btn btn-primary">Cancel</a>
</form>